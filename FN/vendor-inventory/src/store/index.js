// src/store.js
import { configureStore } from "@reduxjs/toolkit";

// Define the root reducer (replace with your slices)
const rootReducer = {};

// Create the Redux store
export const store = configureStore({
  reducer: rootReducer,
});
